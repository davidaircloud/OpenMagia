# generated / machine-specific
config.json
data/
media/
uploads/
server.log
server.pid
install.log
__pycache__/
.DS_Store

# user project state (never publish someone's cuts)
projects/

# heavy, not part of the app (installed by ./install.sh)
h3.c/
models/
addons/
